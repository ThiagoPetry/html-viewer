import React from "react";
import ReactDOM from "react-dom/client";

import ConvertToHtml from "./pages/ConvertToHtml/ConvertToHtml";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <ConvertToHtml />
  </React.StrictMode>
);